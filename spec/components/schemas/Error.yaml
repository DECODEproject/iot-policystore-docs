description: Error message returned by the API
type: object
properties:
  code:
    $ref: '#/components/schemas/ErrorCode'
  msg:
    type: string
    description: Short message describing the error
  meta:
    type: object
    description: >
      Object containing key/value pairs of metadata describing the error in more
      detail
