description: >-
  The interval in seconds over which the moving average should be calculated. It
  is an error to include this value unless the action for the operation is
  `MOVING_AVG`
type: integer
example: 900
